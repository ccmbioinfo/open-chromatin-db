version: '3'

services:
  node:
    build: server
    ports:
      - "3001:3001"
    volumes: 
      - /home/samfeng/open-chromatin-db/data:/usr/src/data
    networks:
      - open-cd
  react:
    build: client
    ports:
      - "3000:3000"
    depends_on: 
      - node
    networks:
      - open-cd

networks:
  open-cd:
    ipam:
      driver: default
      config:
        - subnet: 10.10.11.1/22
